<Window x:Class="MockDesigner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MockDesigner"
        mc:Ignorable="d"
        Width="400" Height="800"
        Title="MainWindow">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="28" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Content="Quote" />
        <Button Grid.Row="0" Grid.Column="1" Content="Customers" />
        <Button Grid.Row="1" Grid.Column="0" Content="Services" />
        <Button Grid.Row="1" Grid.Column="1" Content="Materials" />
        <Button Grid.Row="2" Grid.Column="0" Content="Profile" />
    </Grid>
</Window>